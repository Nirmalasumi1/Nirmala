#include<stdio.h>
int main()
{
int a[size],size,n,i,median;
printf("Enter the size of an array:");
scanf("%d",&size);
printf("Enter how many elements:");
scanf("%d",&n);
printf("Enter the elements of an array:");
scanf("%d"a[i]);
while(n!=0)
{
median=n+1/2;
printf("The median element is:%d");
}
return 0;
}
