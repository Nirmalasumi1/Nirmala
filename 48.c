#include<stdio.h>
int main()
{
int n,a[i],sum=0,avg;
printf("Enter the value of n:");
scanf("%d",&n);
printf("The numbers are:");
scanf("%d",&a[i]);
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
avg=sum/n;
printf("The average is:%d",avg);
return 0;
}
