#include<stdio.h>
int main()
{
int a[10],i,max;
printf("Enter the numbers:");
for(i=0;i<a[i];i++)
scanf("%d",&a[i]);
for(i=0;i<a[i];i++)
{
max=a[0];
if(a[i]>max)
{
max=a[i];
}
printf("The maximum number is %d",max);
}
return 0;
}
