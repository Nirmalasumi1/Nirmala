#include<stdio.h>
int main()
{
int n,a[25],i,max;
printf("Enter how many elements:");
scanf("%d",&n);
printf("Enter the elements:");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
for(i=1;i<n;i++)
{
if(max<a[i])
{
printf("%d",The maximum element is:");
}
}return 0;
}
